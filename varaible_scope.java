public class varaible_scope {
    public static void main(String [] args) {

    // local =  declared inside methode
    //          visible only to that methode

    // gloabl = declared outside a methode, but within a class
    //          visible to all parts of a class

        DiceRoll scope = new DiceRoll();
    }
}
